<div class="">

    <table class="table table-bordered table-hover">
        <thead>
            <tr>
                <th>ID</th>
                <th>Name</th>

                <th>Options</th>
            </tr>
        </thead>
        <tbody >
             <% @categories.each do |category| %>
            <tr >
                <td> <%=category.id %> </td>
                <td>  <%= category.name %> </td>

                <td><%= link_to 'Editar', edit_meal_category_path(meal_category)%> <%= link_to "Excluir", categories_path(meal_category.id), method: :delete%>  </td>
            </tr>
            <% end %>
        </tbody>
    </table>



</div>
